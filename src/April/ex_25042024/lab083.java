package April.ex_25042024;

public class lab083 {
    public static void main(String[] args) {
         //only this are allowed
        // byte. short ,int,long( with is Wrapper type),
        //enums and String.
        //the value for case must be  constant or literal .
        // variables are not allowed
       //break cant be used with elseif condition
       int a =10;
               switch (a){

               }
        char ch = 'A';
       switch (ch){

       }
float f = 30.0F;
       switch ((int) f) { //Narrow - type casting Explicit
       }

       double d= 30.0;
       switch ((int)d) {}
        //boolean b =true;
//        switch (b){ }

  long a11=30l;
       switch ((int)a11){ }
    }
}
